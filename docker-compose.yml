services:
  google-ads-mcp:
    build: .
    expose:
      - "8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true